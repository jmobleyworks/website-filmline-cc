{
  "name": "website-filmline-cc",
  "version": "1.0.0",
  "description": "Website for filmline.cc",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ 2>/dev/null || true",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}